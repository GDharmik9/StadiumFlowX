{
  "name": "stadiumflow-engine",
  "version": "1.0.0",
  "description": "StadiumFlow real-time crowd congestion detection backend engine, deployed on Google Cloud Run with Firebase Firestore.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --coverage",
    "seed": "node seed.js",
    "lint": "npx eslint ."
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.0",
    "firebase-admin": "^12.0.0",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}